# FairShare - Shared Expenses App

FairShare is a production-ready full-stack shared expenses application built to handle messy real-world transaction spreadsheets, dynamic group memberships, multi-currency splits, and debt simplification. It was designed to solve the challenges of shared flatmate expenses, trip spending, and automated CSV export auditing.

## Deployment Link
* Public deployed app: https://sharedexpensesapp.vercel.app

## Project Structure
The repository is structured into two main sub-folders:
* backend - Django API application, database schema, ingestion engine, and import runner script.
* frontend - React 18 single-page application built using Vite, styled with a modern dashboard interface.

Supporting documentation files:
* SCOPE.md - Explains the database schema and the 12 CSV data anomalies handled.
* DECISIONS.md - Decision log for architecture, state management, and algorithmic choices.
* AI_USAGE.md - Documentation of the AI collaborator tool process and corrections.
* IMPORT_REPORT.md - Audit report generated by the CSV ingestion engine.

---

## Core Features

### 1. Ingestion Engine with 12-Anomaly Detection
The backend CSV parser analyzes uploaded files line-by-line and flags 12 distinct spreadsheet anomalies, such as duplicates, currency mismatches, negative amounts, settlements logged as expenses, and members out of active timeline bounds.

### 2. Time-Aware Group Memberships
Rather than splitting expenses statically, membership is date-bounded. For example, if a roommate joined the flat in April, any expenses dated in March (e.g. March electricity bills) will automatically exclude them from the equal split calculation.

### 3. Greedy Transaction Simplification
Features a greedy debt-simplification matching algorithm that computes the absolute minimum number of peer-to-peer transfers required to settle up the group.

### 4. Double-Entry Balance Audit Trail
Provides a complete peer-to-peer line-item relationship list, enabling users to trace exactly which expenses and payments sum up to their net balances.

---

## Detailed Tech Stack
* Backend: Django 4.2 + Django REST Framework + SimpleJWT Token Authentication
* Frontend: React 18 + Vite + Lucide Icons + React Hot Toast notifications
* Styling: Custom Vanilla CSS architecture with absolute layout responsive design
* Database: SQLite (local development) / PostgreSQL (production setup ready via dj-database-url)

---

## Local Setup Instructions

### Prerequisites
* Python 3.11+
* Node.js 18+

### Backend Setup (Django)

1. Navigate to the backend directory:
   cd backend

2. Create and activate a Python virtual environment:
   python -m venv .venv
   # Windows:
   .venv\Scripts\activate
   # macOS / Linux:
   source .venv/bin/activate

3. Install required Python packages:
   pip install -r requirements.txt

4. Set up environment variables:
   Create a .env file in the backend/ directory with the following variables:
   SECRET_KEY=your-secret-key-here
   DEBUG=True
   ALLOWED_HOSTS=localhost,127.0.0.1
   CORS_ALLOWED_ORIGINS=http://localhost:5173

5. Apply database migrations:
   python manage.py migrate

6. Seed mock database and generate the import report:
   python run_import.py

7. Start the development server:
   python manage.py runserver 8000

The backend API will run at http://127.0.0.1:8000.

### Frontend Setup (Vite + React)

1. Navigate to the frontend directory:
   cd frontend

2. Install node packages:
   npm install

3. Run the development server:
   npm run dev

The frontend will run at http://localhost:5173. The configuration proxies API requests automatically to http://127.0.0.1:8000/api/.

---

## Core API Endpoints

### Authentication
* POST /api/register/ - Register a new user account.
* POST /api/token/ - Obtain JWT tokens.
* POST /api/token/refresh/ - Refresh JWT tokens.
* GET /api/users/me/ - Fetch details of the logged-in user.

### Groups & Memberships
* GET, POST /api/groups/ - List user's groups or create a new group.
* GET, PUT, DELETE /api/groups/<uuid>/ - Group detail and operations.
* GET, POST /api/groups/<uuid>/memberships/ - List or add memberships to a group.

### Expenses & Payments
* GET, POST /api/expenses/ - List active expenses or add a new expense.
* GET, PUT, DELETE /api/expenses/<uuid>/ - Edit or soft-delete an expense.
* GET, POST /api/payments/ - List and log payments/refunds.

### CSV Ingestion & Auditing
* POST /api/csv/upload/ - Upload a CSV transaction spreadsheet to start an ingestion job.
* POST /api/csv/anomalies/<uuid>/resolve/ - Approve or reject a detected CSV anomaly.
* GET /api/csv/reports/<uuid>/ - Fetch a detailed status report of a CSV import run.

### Balance Summary
* GET /api/groups/<uuid>/balances/ - Returns individual net balances, simplified debt settlements, and detailed line-item breakdown audit lists.

---

## AI Collaboration Log
This application was co-developed using the Antigravity IDE coding assistant from Google DeepMind. Automated linters, testing frameworks, and type analysis scripts were executed locally to verify all modifications before code submission.
